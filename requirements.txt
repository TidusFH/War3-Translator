# Required dependencies
chardet>=5.0.0
ftfy>=6.0.0

# Translation APIs - BOTH are installed by default
# The tool automatically uses FREE version unless you provide an API key

# Free Google Translate (DEFAULT - no API key needed)
googletrans==4.0.0rc1

# Official Google Cloud Translation API (optional - requires API key in config.ini)
google-cloud-translate>=3.0.0

# How it works:
# 1. No config.ini = Uses free version automatically
# 2. With config.ini + API key = Uses official API automatically
# 3. Both libraries installed = Maximum flexibility!
